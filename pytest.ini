[pytest]
# Test collection for monorepo structure
testpaths = services tests
python_files = test_*.py

markers =
    integration: mark tests that require services/docker-compose
addopts = -m "not integration"
