# Air-gapped deployment: network access disabled by default
ALLOW_NETWORK=false

# Environment placeholders for ai_brain STT/TTS providers
# For air-gapped: use 'none' (default) or local providers: 'vosk' (STT), 'pyttsx3' (TTS)
# For cloud-enabled: use 'openai' (STT), 'gtts' (TTS) - requires ALLOW_NETWORK=true
STT_PROVIDER=none
TTS_PROVIDER=none

# If using an external provider, set its API key here (requires ALLOW_NETWORK=true)
PROVIDER_API_KEY=

# Library of Truth admin key - MUST be set before deployment
# DO NOT use default value in production
LIBRARY_ADMIN_KEY=changeme-admin-REPLACE-THIS

# Gateway URL used by integration tests (leave as-is for local compose)
GATEWAY_URL=http://127.0.0.1:8001
